import { DataPropertyGetterPipePipe } from './data-property-getter-pipe.pipe';

describe('DataPropertyGetterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DataPropertyGetterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
